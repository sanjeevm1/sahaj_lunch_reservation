name: remote ssh command
on:
  push:
    branches: ["main"]

jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
      - name: executing remote ssh commands using password
        run: ssh madhveshs@192.168.1.223

